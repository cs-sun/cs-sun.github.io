[project]
name = "blog"
version = "1.0.0"
description = "Simple static blog generator"
requires-python = ">=3.8"
dependencies = [
    "markdown>=3.5",
    "jinja2>=3.1",
    "pyyaml>=6.0",
]

[project.scripts]
build-blog = "build_blog:build_blog"
